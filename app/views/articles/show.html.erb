<div class="container">
  <br>
  <div class="article-body">
    <p><%= attachment_image_tag(@article, :user_image, size: "200x150", fallback: "no_image.jpg") %></p>
    <p>投稿日時：<%= @article.created_at.strftime('%Y/%m/%d') %></p>
    <p>タイトル：<%= @article.title %></p>
    <p>紹介：<%= @article.body %></p>
    <!--<p>ユーザーネーム：<= @article.user.name %></p>-->
  </div>
</div>